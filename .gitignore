# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cloudflare Worker (สถานะ local ของ wrangler dev)
worker/.wrangler
.dev.vars

# ผลลัพธ์ของ scripts/gen-users.mjs — มีรหัสผ่าน/แฮชของพนักงานจริง ห้ามขึ้น git
worker/seed/
# ไฟล์รายชื่อองค์กรที่ใช้ป้อนสคริปต์ — มีชื่อ/อีเมลพนักงานจริง ห้ามขึ้น git
*.users.csv
*Employees*.csv
